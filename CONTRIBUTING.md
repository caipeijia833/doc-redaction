# Contributing / 贡献指南

感谢参与。本项目当前是单机离线 Alpha，贡献应优先保证失败关闭、隐私边界、格式可验证和许可证可追溯。

## 基本要求

1. 不要在 Issue、PR、测试夹具或日志中提交真实个人信息、法律卷宗、密钥、Token、口令或可还原密文。
2. 测试数据应完全合成，或具有明确的公开再分发许可证并保留来源、固定版本、SHA-256 和许可证文本。
3. 新增规则必须同时包含正例、反例、边界情况和中英文界面名称；具有校验码的标识符不得只靠正则判断。
4. 文档解析失败、残留复扫失败或输出无法验证时，应失败关闭，不得静默返回原文件。
5. 不得引入 AGPL、来源不明模型、未固定版本下载或绕过现有禁用依赖策略。

## 提交前检查

干净克隆的基础验证入口：

```powershell
.\build-windows.ps1
```

固定 OCR/媒体组件和合成样例齐备后的维护者验证入口：

```powershell
.\build-windows.ps1 -WithNativeTests
.\audit\run-local-security-gate.ps1
```

完整媒体/OCR 测试依赖来源清单中固定版本的本地运行时。`audit/verify-upstream.ps1` 只用于维护者核对被 Git 忽略的固定上游调研镜像，不是普通贡献前置条件。无法执行的目标环境测试必须在 PR 中明确列为未验证，不能写成通过。

提交信息应说明改动目的、风险、测试证据、许可证影响及仍未覆盖的边界。贡献一经提交，即按根目录 Apache License 2.0 许可。
